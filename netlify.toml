# Next.js on Netlify – plugin auto-sets publish dir and server
# Set in Netlify UI: DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
